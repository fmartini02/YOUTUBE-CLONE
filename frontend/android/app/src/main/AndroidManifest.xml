<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- enableOnBackInvokedCallback: il tasto Indietro e la gesture passano
         dall'OnBackPressedDispatcher (vedi MainActivity). Su Android 13/14 va
         chiesto esplicitamente; da Android 15 con targetSdk 35+ è già così. -->
    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <!-- Cast SDK nativo: il framework istanzia da sé CastOptionsProvider
             leggendo questo nome, e CastContext.getSharedInstance() non parte
             senza. Vedi CastBridgePlugin. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.ytproxy.app.CastOptionsProvider" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Scoperta e sessione Cast (play-services-cast-framework).

         Il Cast SDK trova i dispositivi via mDNS. Su Android 13+ (targetSdk 36)
         la scoperta Wi-Fi è dietro NEARBY_WIFI_DEVICES, che è un permesso
         "runtime": va anche chiesto a mano (lo fa CastBridgePlugin alla prima
         apertura del selettore). Senza, il selettore resta vuoto anche con un
         Chromecast acceso sulla stessa rete — NB: le Smart TV senza Chromecast
         integrato (Samsung, LG, Roku, Fire TV) non compaiono comunque, non
         parlano il protocollo Google Cast.
         CHANGE_WIFI_MULTICAST_STATE serve a ricevere i pacchetti mDNS multicast.
         ACCESS_FINE_LOCATION è il ripiego pre-13 (cap a maxSdkVersion=32, così
         su 13+ non chiede la posizione). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
</manifest>
